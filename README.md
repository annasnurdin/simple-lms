# React + TypeScript

Simple LMS Guru dan Siswa.

### 1. Mock API

Untuk memulai, jalankan dulu `json-server` untuk **mock API nya**

- **Command:**
  ```bash
  npx json-server --watch base.json
  ```

### 2. Run

- **Command:**
  ```bash
  npm run dev
  ```
